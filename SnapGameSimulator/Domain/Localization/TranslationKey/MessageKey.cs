namespace SnapGameSimulator.Domain.Localization
{
    /// <summary>
    /// Enum representing the keys for localized messages.
    /// </summary>
    public enum MessageKey
    {
        EnterPackLimit,
        InvalidPackLimit,
        Player,
        WelcomeMessage,
        Loading,
        TotalPlayers,
        DiscardedMessage,
        Result,
        PileWinner,
        TotalCards,
        SnapMessage,
        Winner,
    }
}
